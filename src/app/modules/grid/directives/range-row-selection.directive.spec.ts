/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { RangeRowSelectionDirective } from './range-row-selection.directive';

describe('Directive: RangeRowSelection', () => {
  it('should create an instance', () => {
    const directive = new RangeRowSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
